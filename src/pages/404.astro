---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 — Miguel Pereira" description="Page not found.">
  <div class="e404">

    <div class="e404-glow" aria-hidden="true"></div>
    <div class="e404-scanline" aria-hidden="true"></div>

    <nav class="nav">
      <div class="nav-inner">
        <a href="/" class="nav-logo">miguel<span>.</span>pereira</a>
        <ul class="nav-links">
          <li><a href="/#about">About</a></li>
          <li><a href="/writing">Writing</a></li>
          <li><a href="/conferences">Events</a></li>
          <li><a href="/cv">CV</a></li>
          <li><a href="https://www.linkedin.com/in/miguelpereira25/" target="_blank" rel="noopener">Contact</a></li>
        </ul>
      </div>
    </nav>

    <main class="e404-main">

      <div class="glitch-wrap" aria-label="404">
        <span class="glitch" data-text="404">404</span>
      </div>

      <p class="e404-sub">Route not found</p>

      <div class="terminal" role="region" aria-label="Error trace">
        <div class="terminal-bar">
          <span class="t-dot t-red"></span>
          <span class="t-dot t-yellow"></span>
          <span class="t-dot t-green"></span>
          <span class="t-title">bash — miguelpereira.org</span>
        </div>
        <div class="terminal-body">
          <div class="t-line"><span class="t-prompt">$</span> traceroute miguelpereira.org</div>
          <div class="t-line t-dim">traceroute to miguelpereira.org, 30 hops max, 60 byte packets</div>
          <div class="t-line"><span class="t-hop">1</span>  gateway (192.168.0.1) &nbsp;&nbsp; 1.204 ms</div>
          <div class="t-line"><span class="t-hop">2</span>  10.0.0.1 &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; 8.441 ms</div>
          <div class="t-line"><span class="t-hop">3</span>  <span class="t-warn">* * *</span></div>
          <div class="t-line t-spacer"></div>
          <div class="t-line"><span class="t-prompt">$</span> curl -I https://miguelpereira.org<span id="current-path" class="t-acc"></span></div>
          <div class="t-line t-dim">HTTP/2  <span class="t-err">404</span>  Not Found</div>
          <div class="t-line t-dim">server: astro/static</div>
          <div class="t-line t-dim">content-type: text/html</div>
          <div class="t-line t-spacer"></div>
          <div class="t-line"><span class="t-prompt">$</span> <span class="t-cursor">▊</span></div>
        </div>
      </div>

      <div class="e404-ctas">
        <a href="/" class="btn btn-primary">&larr; go home</a>
        <a href="/#writing" class="btn btn-ghost">read my writing</a>
      </div>

    </main>

    <div class="utc-clock" aria-live="polite">
      <span class="utc-label">UTC</span>
      <span id="utc-time">--:--:--</span>
    </div>

  </div>
</Layout>

<style>
  .e404 {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    background: var(--bg);
  }

  .e404-glow {
    position: fixed;
    inset: 0;
    background: radial-gradient(
      ellipse 50% 40% at 50% 40%,
      rgba(0, 212, 255, 0.03) 0%,
      rgba(123, 92, 245, 0.015) 30%,
      transparent 70%
    );
    pointer-events: none;
    z-index: 0;
  }

  .e404-scanline {
    position: fixed;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(0, 212, 255, 0.015) 50%,
      transparent 100%
    );
    background-size: 100% 200px;
    animation: scanline 7s linear infinite;
    pointer-events: none;
    z-index: 0;
  }

  @keyframes scanline {
    0%   { background-position: 0 -200px; }
    100% { background-position: 0 100vh; }
  }

  .e404-main {
    position: relative;
    z-index: 1;
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px 24px 80px;
    gap: 28px;
  }

  .glitch-wrap {
    position: relative;
    user-select: none;
  }

  .glitch {
    font-family: var(--font-serif);
    font-size: clamp(7rem, 22vw, 16rem);
    font-weight: 400;
    letter-spacing: -0.03em;
    line-height: 1;
    color: var(--text);
    position: relative;
    display: block;
  }

  .glitch::before,
  .glitch::after {
    content: attr(data-text);
    position: absolute;
    inset: 0;
    font-family: inherit;
    font-size: inherit;
    font-weight: inherit;
    letter-spacing: inherit;
    line-height: inherit;
  }

  .glitch::before {
    color: #e05050;
    animation: glitch-before 4s infinite steps(1);
    clip-path: polygon(0 15%, 100% 15%, 100% 40%, 0 40%);
  }

  .glitch::after {
    color: var(--accent);
    animation: glitch-after 4s infinite steps(1);
    clip-path: polygon(0 60%, 100% 60%, 100% 80%, 0 80%);
  }

  @keyframes glitch-before {
    0%,  92%  { transform: translate(0); opacity: 0; }
    93%        { transform: translate(-3px, 2px); opacity: 1; }
    94%        { transform: translate(3px, -1px); opacity: 1; }
    95%        { transform: translate(-2px, 0); opacity: 1; clip-path: polygon(0 5%, 100% 5%, 100% 25%, 0 25%); }
    96%        { transform: translate(0); opacity: 0; }
    100%       { opacity: 0; }
  }

  @keyframes glitch-after {
    0%,  90%  { transform: translate(0); opacity: 0; }
    91%        { transform: translate(2px, -1px); opacity: 1; }
    92%        { transform: translate(-2px, 2px); opacity: 1; clip-path: polygon(0 55%, 100% 55%, 100% 75%, 0 75%); }
    93%        { transform: translate(0); opacity: 0; }
    100%       { opacity: 0; }
  }

  .e404-sub {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--muted);
    margin-top: -12px;
  }

  .terminal {
    width: 100%;
    max-width: 560px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    font-family: var(--font-mono);
    font-size: 0.8rem;
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    background: rgba(255,255,255,0.02);
    border-bottom: 1px solid var(--border);
  }

  .t-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }
  .t-red    { background: #e05050; }
  .t-yellow { background: #e0b050; }
  .t-green  { background: #50c070; }

  .t-title {
    margin-left: 8px;
    font-size: 0.72rem;
    color: var(--muted);
    letter-spacing: 0.04em;
  }

  .terminal-body {
    padding: 16px 18px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    line-height: 1.7;
  }

  .t-line { color: var(--text); white-space: pre; }
  .t-spacer { height: 6px; }
  .t-dim  { color: var(--muted); }
  .t-prompt { color: var(--accent); margin-right: 8px; }
  .t-hop  { color: var(--muted); display: inline-block; width: 14px; }
  .t-warn { color: #e0b050; }
  .t-err  { color: #e05050; font-weight: 600; }
  .t-acc  { color: var(--accent); }

  .t-cursor {
    color: var(--accent);
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0; }
  }

  .e404-ctas {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .utc-clock {
    position: fixed;
    bottom: 20px;
    right: 24px;
    z-index: 10;
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--muted);
    letter-spacing: 0.08em;
  }

  .utc-label {
    color: var(--accent);
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    border: 1px solid var(--border-accent);
    padding: 1px 5px;
    border-radius: 3px;
  }

  @media (max-width: 720px) {
    .glitch { font-size: clamp(5rem, 28vw, 7rem); }
    .terminal { font-size: 0.72rem; }
  }
</style>

<script>
  const pathEl = document.getElementById('current-path');
  if (pathEl) pathEl.textContent = window.location.pathname;

  const clockEl = document.getElementById('utc-time');
  function tick() {
    if (!clockEl) return;
    const now = new Date();
    const hh = String(now.getUTCHours()).padStart(2, '0');
    const mm = String(now.getUTCMinutes()).padStart(2, '0');
    const ss = String(now.getUTCSeconds()).padStart(2, '0');
    clockEl.textContent = `${hh}:${mm}:${ss}`;
  }
  tick();
  setInterval(tick, 1000);
</script>
</Layout>
